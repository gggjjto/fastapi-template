# Business Web App

Minimal Next.js App Router workspace for product-facing features.

Run from the repository root:

```bash
make web-dev
make web-lint
make web-typecheck
make web-build
```

The development server runs at `http://localhost:3000`.
